# ============================================================
# CWI Ingestion Pipeline — Environment Variables
# Copy to scripts/.env and fill in your values.
# ============================================================

# OpenAI (for text-embedding-3-small)
OPENAI_API_KEY=sk-...

# Supabase (use the SERVICE ROLE key — not the anon key)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# OneDrive / Microsoft Graph (only needed for --source=onedrive)
# Register an app at portal.azure.com → Azure Active Directory → App registrations
# Permissions required: Files.Read.All (or Files.ReadWrite.All for write access)
ONEDRIVE_CLIENT_ID=your-azure-app-client-id
ONEDRIVE_CLIENT_SECRET=your-azure-app-client-secret
ONEDRIVE_TENANT_ID=your-azure-tenant-id
ONEDRIVE_FOLDER_PATH=/CWP Precedents
