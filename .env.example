# ============================================================
# Clearwater Partners — Frontend Environment Variables
# ============================================================
# Copy to .env and fill in your values.
# NEVER commit the .env file — it is git-ignored.
# All VITE_ vars are safe for the browser (public).
# ============================================================

# ── Supabase (required for full CWI RAG system) ─────────────
# Supabase Dashboard → Project → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# ── Gemini (fallback — used if Supabase is not yet configured)
# https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# ── WordPress (optional — Precedent hub CMS integration) ─────
# VITE_WP_API_URL=https://your-wordpress-install.com/wp-json
